name: demo pipeline

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello, World!"
      
  copy:
    runs-on: ubuntu-latest
    needs: hello
    steps:
      - name: Copy file 
        run: cd home | cp -r sandesh sandesh_copy 
      
